apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: loki-vs
  namespace: observability
spec:
  hosts:
  - "loki.observability.local"
  gateways:
  - observability-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: loki.observability.svc.cluster.local
        port:
          number: 3100
